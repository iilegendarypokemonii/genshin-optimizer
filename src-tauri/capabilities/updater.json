{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Only the main local app window can check and install signed updates.",
  "windows": ["main"],
  "permissions": ["updater:default"]
}
